# Prisma CLI needs DATABASE_URL in .env (not .env.local)
# This file is read by: npx prisma db push, npx prisma studio, etc.
# Next.js reads .env.local which also has this variable.
DATABASE_URL="file:./prisma/dev.db"
